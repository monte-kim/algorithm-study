function solution(seoul) {
  // var answer = '';
  // const x = seoul.indexOf(seoul.find((el) =>         el === "Kim"))
  // answer = `김서방은 ${x}에 있다`;
  // return answer;
  return `김서방은 ${seoul.indexOf('Kim')}에 있다`;
}
